const VERSION='v30-marketplace-os',CATALOGUE='https://khzpdyyywiucfhubxkev.supabase.co/functions/v1/perkdrop-catalogue-api?limit=200',TRANSIENT=new Set([408,429,500,502,503,504]);
async function fetchCatalogue(){let lastError=null;for(let attempt=0;attempt<2;attempt++){try{const r=await fetch(CATALOGUE,{headers:{accept:'application/json'},signal:AbortSignal.timeout(9000)});if(r.ok||!TRANSIENT.has(r.status)||attempt===1)return r;console.warn(JSON.stringify({msg:'catalogue_dependency_retry',route:'/api/health',attempt:attempt+1,upstreamStatus:r.status}));await r.arrayBuffer().catch(()=>{});}catch(error){lastError=error;if(attempt===1)throw error;console.warn(JSON.stringify({msg:'catalogue_dependency_retry',route:'/api/health',attempt:attempt+1,errorName:error?.name||'unknown'}));}await new Promise(resolve=>setTimeout(resolve,150));}throw lastError||new Error('catalogue_unavailable')}
module.exports=async function handler(req,res){const started=Date.now(),requestId=String(req.headers['x-vercel-id']||req.headers['x-request-id']||`health-${started}`).slice(0,180);let catalogue={ok:false,liveDrops:null,apiVersion:null};try{const r=await fetchCatalogue();if(!r.ok){console.warn(JSON.stringify({msg:'catalogue_dependency_failed',route:'/api/health',requestId,upstreamStatus:r.status,upstreamRequestId:r.headers.get('x-perkdrop-request-id')||null,upstreamFailure:r.headers.get('x-perkdrop-query-failure')||null,upstreamQueryMs:r.headers.get('x-perkdrop-query-ms')||null,ms:Date.now()-started}));}else{const b=await r.json(),deals=Array.isArray(b)?b:(b.deals||[]);catalogue={ok:true,liveDrops:deals.length,apiVersion:b.apiVersion||b.version||null}}}catch(error){console.warn(JSON.stringify({msg:'catalogue_dependency_error',route:'/api/health',requestId,errorName:error?.name||'unknown',ms:Date.now()-started}));}const ok=catalogue.ok;res.status(ok?200:503).setHeader('Cache-Control','no-store').setHeader('X-PerkDrop-Request-Id',requestId).json({ok,status:ok?'healthy':'degraded',service:'perkdrop',version:VERSION,environment:process.env.VERCEL_ENV||process.env.NODE_ENV||'production',catalogue,timestamp:new Date().toISOString()})}
