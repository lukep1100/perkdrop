const VERSION='v42-release-core';
const CACHE=`perkdrop-${VERSION}`;
const STATIC=['/app.js?v=v42-release-core','/discovery-rules.mjs?v=v39-venue-rails','/styles.css?v=v42-release-core','/images/venue-unavailable.svg','/icon.svg','/manifest.webmanifest'];
// Cache public assets only. Availability, authenticated screens and passes are never cached.
self.addEventListener('install',e=>e.waitUntil(caches.open(CACHE).then(c=>c.addAll(STATIC)).then(()=>self.skipWaiting())));
self.addEventListener('activate',e=>e.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k.startsWith('perkdrop-')&&k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim())));
self.addEventListener('fetch',e=>{if(e.request.method!=='GET')return;const u=new URL(e.request.url);if(u.origin!==location.origin)return;if(STATIC.includes(u.pathname+u.search)){e.respondWith(fetch(e.request).then(r=>{const copy=r.clone();caches.open(CACHE).then(c=>c.put(e.request,copy));return r;}).catch(()=>caches.match(e.request)));return}if(e.request.mode==='navigate')e.respondWith(fetch(e.request).catch(()=>new Response('<!doctype html><meta name="viewport" content="width=device-width,initial-scale=1"><title>PerkDrop — Offline</title><style>body{background:#08090e;color:#fff;font:18px Arial;padding:30px}a{color:#ff4db8}</style><h1>You’re offline</h1><p>Reconnect to see current deals and availability.</p><a href="/">Try again</a>',{status:503,headers:{'Content-Type':'text/html; charset=utf-8','Cache-Control':'no-store'}})))});
